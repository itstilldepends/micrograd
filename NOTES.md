# 学习笔记

> 个人学习记录。Claude 只在我明确说"记一下/改 NOTES"时才动这个文件。

---

## 进度

### 视频进度
- [ ] 0:00 看了导数的数值定义
- [ ] 25min Value 类、加法乘法
- [ ] 55min 手动反向传播
- [ ] 1:15 闭包自动反传
- [ ] 1:35 拓扑排序
- [ ] 1:45 tanh / 拆解
- [ ] 2:00 nn.py
- [ ] 2:15 训练 demo

### 代码掌握度
- [ ] 能不看代码默写出 `Value.__add__` 的 forward + `_backward`
- [ ] 能解释拓扑排序为什么要 DFS + 后序
- [ ] 能给 `Value` 加一个新 op(比如 `tanh` / `exp` / `sigmoid`),并自己验证梯度
- [ ] 能跑通 `demo.ipynb` 并解释每一步在做什么
- [ ] 能解释 `test_engine.py` 是怎么用 PyTorch 当 oracle 的

---

## 核心概念笔记

### autograd / 反向传播
<!-- 比如:为什么 grad 要 +=、拓扑排序为什么、backward 的 self.grad=1 含义 -->

### Python 机制
<!-- dunder methods、closure、lambda、生成器、运算符重载等,记自己版本的理解 -->

### nn 库(Neuron / Layer / MLP)
<!-- 网络结构、parameters() 扁平化、最后一层为什么不加非线性等 -->

### 训练 / demo.ipynb
<!-- loss 设计、SGD 步骤、batch 处理等 -->

---

## 问答精华

<!-- 自己觉得有价值的对话:问题 + 关键答案。一条一段,留时间戳。
格式示例:

### Q: dunder/闭包/lambda 是什么 (2026-05-25)
- dunder = 双下划线方法,Python 把 `a+b` 翻译成 `a.__add__(b)`
- lambda = 一行匿名函数,micrograd 里只用作 `_backward` 的默认占位
- closure = 嵌套函数捕获外层变量的"引用",这是 `_backward` 能在 `__add__` 返回后还读到正确 `out.grad` 的根因
-->

### Q: 函数怎么能当值传?内存里长啥样? (2026-05-30)
- **函数是对象**:能赋值/传参/返回。`out._backward = _backward`(engine.py:20)没括号 = 存函数本身,有括号才是调用。
- **传函数 = 传地址**:函数体躺在内存某处,变量只存它的地址(地址就是个整数),不复制、不序列化。
- **代码 = 一串字节**:C 的 `add` 编译后 = 32 字节机器码,和数据字节没区别,只是被 CPU 当指令执行。
- **Python 多一层**:函数是个对象,里面装字节码(`add.__code__.co_code`),解释器跑;`id(add)` = 对象地址。
- **一等函数 ≠ 所有语言都有**:Go/C++ 能;Java 函数非一等、要包一层(设计理念差异)。
- **彩蛋**:`a+b` 的字节码 `BINARY_OP +` 会调左边的 `__add__` → 这就是 `Value.__add__`(engine.py:13)被触发的根因。

### Q: 拓扑排序是什么 / 用于什么 / 怎么实现 (2026-05-30)
- **是什么**:对 DAG 排序,使每条边都从"前"指向"后"(A 依赖 B 则 B 排在前)。按**连接结构**排,不是按节点值排——这就是"拓扑"二字的由来(数学拓扑 = 研究连接关系/结构)。
- **偏序→全序**:DAG 是偏序(有些节点不可比,如 micrograd 里 `a` 和 `c`),拓扑排序把它补成一条线(linear extension)。所以合法解通常**不唯一**。
- **用于**:构建系统(Make/Bazel)、包管理依赖、课程先修(LeetCode 207)、任务调度、电子表格重算、反向传播、**循环依赖/死锁检测**。
- **顺带判环**:能排完 = DAG;排不完(有剩余节点)= 有环。
- **实现 A:DFS 法**(micrograd 用的,engine.py:59):递归处理完所有孩子后再 append 自己 → 输入在前输出在后,反传时 `reversed(topo)`。坑:递归深度=图深度,链太长会**爆栈**,生产代码常改迭代版。
- **实现 B:Kahn 法(入度/BFS)**:算每个节点入度,入度0的入队;反复取队首、把它指向的节点入度-1、减到0就入队。天然判环(`len(topo)!=N` 即有环);想要字典序最小就把队列换成最小堆。
- **两者都 O(V+E)**。面试要点:两种都会写 + 主动说"它同时是判环算法""DFS 递归版有爆栈风险"。
- micrograd 的 `visited` set(engine.py:58)只为去重(共享节点如 `a+a` 只加一次),**不判环**——每次运算 new 新 `out`,图天然无环。

> 🧵 主题:反传累乘 vs 累加(三连问,现象→数学→本质,建议连读)

### Q: 反传是累加还是累乘? (2026-05-31) (1/3)
- **两个都对,在不同的"轴"上**。看 `self.grad += other.data * out.grad`(engine.py:29)一行就够:右边 `*` 是累乘,左边 `+=` 是累加。
- **累乘 = 沿一条路径(链式法则)**:`局部导数 × out.grad`。链 `a→b→c→L` 的 `dL/da = dL/dc·dc/db·db/da`,纯连乘。→ 这就是**梯度消失/爆炸**的根因(一长串局部导数连乘,<1 指数衰减,>1 指数膨胀)。
- **累加 = 多条路径汇合到同一节点(多元链式法则)**:节点被用多次(fan-out)时,每条边回灌一份梯度要加起来。例 `a` 喂给 `e=a*b` 和 `f=a*c`,`dL/da = dL/de·de/da + dL/df·df/da`,靠 `+=` 累加。
- 这就是「为什么 `+=` 不是 `=`」+「为什么每个节点 grad 从 0 起」的根本原因。
- 别只记成"反传是加法"——主体是链式法则的**乘**,`+=` 只负责把同一节点的多条出边贡献合并。

### Q: 这跟多元链式法则的关系? (2026-05-31) (2/3)
- **`+=` 累加就是多元链式法则本身**,不是工程技巧,是数学公式直译成代码。
- **单变量链式法则**:`L=f(y), y=g(x) → dL/dx = dL/dy · dy/dx`,一条链直接乘 → 对应"沿路径累乘"。
- **多元链式法则**:x 经多条路径影响 L(x 喂给 u₁…uₙ)→ `dL/dx = Σᵢ (∂L/∂uᵢ)(∂uᵢ/∂x)`。那个 **Σ** 就是累加的出处。
- **为什么求和**:抖动 x 一点,扰动同时顺各路径传到 L;一阶近似下独立路径线性叠加,故各项相加。
- **逐项对回代码**(`a` 喂 `e=a*b`、`f=a*c`):`dL/da = e.grad·b + f.grad·c`。`child.grad += 局部导数 * out.grad`(engine.py:29)= 求和里的一项(`out.grad`=∂L/∂uᵢ,局部导数=∂uᵢ/∂x);`+=` = 公式的 Σ。
- **三件事拼成完整公式**:拓扑排序保证每项 `∂L/∂uᵢ` 用时已算好;`+=` 负责求和;`* out.grad` 负责单路径链式相乘。
- **面试加分**:反传 = 多元链式法则的高效实现;**为什么高效**——复用 `out.grad`(动态规划式缓存上游结果),O(图大小) 而非 O(路径数,可能指数级)。

### Q: 为什么"一阶近似下独立路径线性叠加"? 累加和累乘同源吗? (2026-05-31) (3/3)
- **同一个根:导数=局部线性近似(增益)**。但累乘/累加用的是"线性"的**两条不同性质**。
- **线性 = 齐次性 + 可加性**:
  - **齐次性** `f(ax)=a·f(x)` → "增益"概念 → 串联复合时增益**相乘** → **累乘**(两级放大器串联,总倍数相乘)。
  - **可加性/叠加(superposition)** `f(x₁+x₂)=f(x₁)+f(x₂)` → 输入相加则输出相加 → 多路径汇合时**累加**。
- **"独立路径线性叠加"的精确含义**:路径间的耦合是**二阶小量**,求导取极限时消失。例 `L=u·v`(u、v 都依赖 x):
  `ΔL = vΔu + uΔv + ΔuΔv`,前两项=各路径一阶贡献(相加),`ΔuΔv`=耦合(二阶)。÷Δx 取极限,二阶项→0 → `dL/dx = v·du/dx + u·dv/dx`。
  - 彩蛋:这条正是乘积法则 `(uv)'=u'v+uv'`,它其实是多元链式法则的特例。
- **统一视角(面试可甩)**:梯度=线性映射(多维=Jacobian)。线性映射两种行为正好是**矩阵乘法**结构 `(AB)_{ij}=Σ_k A_{ik}B_{kj}`:`A_{ik}·B_{kj}` 的**乘**=沿边累乘,对共享下标 k 的 **Σ**=节点汇合累加。
- 所以多元链式法则 = Jacobian 连乘;累乘/累加 = 矩阵乘法的两个组成部分。PyTorch 张量版就是真的在做 Jacobian 乘法(向量化,不显式建大矩阵)。
- **一句话**:反传成立是因为求导把非线性 op 局部线性化,线性世界里"串联相乘、并联相加"。

---

## TODO / 还没搞懂的

<!-- 留这里的问题,下次继续问 -->
